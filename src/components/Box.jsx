import React from "react";
import styled from 'styled-components';

const StyledBox = styled.div`
    display: inline-block;
    width: 15px;
    height: 15px;

    ${props => {
        
    }}
`

const Box = ({number, isFlag, isMine}) => {
    return (
        
    )
}

export default Box;